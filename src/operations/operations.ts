import { and } from "./and";
import { or } from "./or";
import { xor } from "./xor";
import { not } from "./not";
import { nand } from "./nand";
import { nor } from "./nor";

export const logicalOperations = {
    AND: and,
    OR: or,
    XOR: xor,
    NOT: not,
    NAND: nand,
    NOR: nor
}